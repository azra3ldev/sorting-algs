#include <stdbool.h>

#include "../extra/aux.h"

/*
 * coktail sort is a modified version of bubble sort
 * that also traverses back after every iteration
 */
void cocktail_sort(int* array, int size);
